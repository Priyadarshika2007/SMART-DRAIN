# Local development overrides (not committed to git)
# This file takes precedence over .env during development

# Default to deployed backend for cross-device access.
REACT_APP_API_URL=https://smart-drain-backend.onrender.com/api

# Show verbose logs during development
REACT_APP_DEBUG=true
